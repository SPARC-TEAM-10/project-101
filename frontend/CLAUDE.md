# Frontend standards

> Fill in once the frontend stack is locked (Architect agent does this).
> Only loaded by agents working inside `frontend/` — keep it scoped to
> frontend concerns. Shared/cross-cutting rules live in the root `CLAUDE.md`.

## Stack

- **Language/framework**: [PLACEHOLDER — e.g. React + Vite + TypeScript]
- **State/data fetching**: [PLACEHOLDER]
- **Key libraries**: [PLACEHOLDER]

## Coding standards

- Language/style guide: [PLACEHOLDER — e.g. Airbnb JS/TS style]
- Linter/formatter: [PLACEHOLDER — e.g. eslint + prettier]
- Test framework: [PLACEHOLDER — e.g. vitest, jest + testing-library]

## Frontend-specific definition of done

In addition to the root `CLAUDE.md` DoD checklist:

- [ ] Consumes the backend API exactly as documented in the contract (no
      guessed/undocumented fields)
- [ ] [PLACEHOLDER — e.g. no console errors/warnings, basic a11y checks pass]

## Interfaces consumed from backend

[PLACEHOLDER — filled in by the Architect: API routes/contract this module
consumes, or a link to the OpenAPI spec / Confluence page]
