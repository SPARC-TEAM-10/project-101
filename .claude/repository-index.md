# Module Index

> Auto-generated by the Startup Agent. Re-run startup to refresh.
> Last updated: 2026-09-02

| Module | Path | Description |
|---|---|---|
| backend | backend/ | ASP.NET Core 8 Web API — see backend/CLAUDE.md. `[NOT SCAFFOLDED]` — no project files yet, stack described but not created |
| frontend | frontend/ | React + TypeScript + Vite web app — see frontend/CLAUDE.md. `[NOT SCAFFOLDED]` — no project files yet, stack described but not created |

> **Note:** Each row represents a **module folder within this single repo**,
> not a separate git repository. There is exactly one git repository — this
> one (`github.com/SPARC-TEAM-10/hackathon-agent-scaffold`). Agents use this
> file to confirm a module folder exists before referencing paths inside it;
> one feature branch is created per task, in this repo, per `orchestrator.md`.
