# Chh.Application / Validators

FluentValidation 11 validators, one per request DTO. Registered by assembly scan
from `Chh.Api/Extensions`.

Empty by design — validators follow their DTOs, which follow the contract.
