# Chh.Application / Queries

Read-side handlers. Read-only data access must use `AsNoTracking()` at the
repository level and return paginated results per `.claude/rules/api-standards.md` §6.

Empty by design.
