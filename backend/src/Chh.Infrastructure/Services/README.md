# Chh.Infrastructure / Services

Infrastructure implementations of `Chh.Application/Contracts` interfaces —
repositories, Hangfire background jobs (proximity notification fan-out, PRD NFR
dispatch < 5s), clock/caching adapters.

Empty by design.
