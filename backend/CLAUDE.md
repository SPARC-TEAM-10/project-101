# Backend standards

> Fill in once the backend stack is locked (Architect agent does this).
> Only loaded by agents working inside `backend/` — keep it scoped to
> backend concerns. Shared/cross-cutting rules live in the root `CLAUDE.md`.

## Stack

- **Language/framework**: [PLACEHOLDER — e.g. Python + FastAPI, Node + Express]
- **Database**: [PLACEHOLDER]
- **Key libraries**: [PLACEHOLDER]

## Coding standards

- Language/style guide: [PLACEHOLDER — e.g. PEP8, Google Go style]
- Linter/formatter: [PLACEHOLDER — e.g. ruff, eslint]
- Test framework: [PLACEHOLDER — e.g. pytest, jest]

## Backend-specific definition of done

In addition to the root `CLAUDE.md` DoD checklist:

- [ ] API endpoints match the documented contract (OpenAPI/Confluence) exactly
- [ ] [PLACEHOLDER — e.g. DB migrations are reversible, no breaking schema changes without a migration plan]

## Interfaces exposed to frontend

[PLACEHOLDER — filled in by the Architect: API routes/contract this module
exposes, or a link to the OpenAPI spec / Confluence page]
